class APITimeouts {
  final Duration normalTimeout = const Duration(seconds: 5);
  final Duration mediumTimeout = const Duration(seconds: 10);
  final Duration longTimeout = const Duration(seconds: 20);
}
