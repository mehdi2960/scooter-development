name: web_service
publish_to: 'none'

environment:
  sdk: '>=2.19.0 <3.0.0'
  flutter: '>=3.7.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  custom_logger:
    path: ../custom_logger

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
